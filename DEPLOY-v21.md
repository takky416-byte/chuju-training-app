# v21 デプロイ手順

3つの改善を行いました。Cloud Functionsの変更はありません（`functions`のデプロイは不要です）。

## 変更内容

### 1. 問題の一括追加で複数ファイルを同時に選択できるように

- 「JSONファイルを選択」が複数選択に対応しました。適性検査・基礎トレのファイルを混ぜて選んでも、それぞれ自動判別して個別にプレビューされます。
- ファイルごとに読込数・新規・既存ID・エラーを表示し、問題のあるファイルだけを「×」で取り消してから残りを登録できます。
- 「既存IDの扱い（スキップ／上書き）」は選択中のすべてのファイルに共通で適用されます。
- 複数ファイルの中で同じIDが重複している場合は、該当するすべてのファイルにエラーとして表示され、登録がブロックされます（取り消すか、IDを直してから再度選択してください）。
- 一括追加ボタンは、選択中のファイル全体の合計問題数を対象に、まとめて1回で登録します。

### 2. 漢字の書き：「答えを見る」でAI判定も同時に実行

- 前回追加した「AI判定」ボタンを廃止し、「答えを見る」を押した時点で自動的にAI（Cloud Vision API）が手書き文字を読み取るようにしました。
- 読み取り結果は、正解の答えのすぐ下に「AIの読み取り：「○○」・答えと一致／不一致（プロトタイプ・参考用）」として表示されます。
- 最終的な採点は今まで通り「できた／まちがえた」の自己申告のままです（AIは判定を確定させません）。

### 3. 漢字の書き：「答えを見る」ボタンを縮小し、筆記スペースを拡大

- タブレット横向き表示で、「答えを見る」ボタンの幅を縮め、その分だけ筆記欄（漢字を書くスペース）を広げました。
- ボタンの文字は「答えを」「見る」の2行表示にしました。

## デプロイ

```bash
npm install
npm run build
npx firebase-tools deploy --only hosting --project aichi-jh-training-507310
```

（Cloud Functionsに変更はないため`functions`はデプロイ対象から外しています。）

## 確認項目

- JSONファイルを2つ以上同時に選択すると、ファイルごとにプレビューカードが表示される
- 1つのファイルにエラーがあっても、「×」で取り消せば残りのファイルだけで一括追加できる
- 複数ファイルで同じIDが重複している場合、該当ファイルにエラーとして表示される
- 一括追加を押すと、選択した全ファイルの対象問題がまとめて登録される
- 漢字の書き問題で「答えを見る」を押すと、答えの下にAIの読み取り結果が表示される
- 「できた／まちがえた」の自己採点・履歴保存は従来通り動作する
- タブレット横向きの漢字問題で、筆記欄が広くなり、「答えを見る」ボタンが縦2行表示で右側に収まっている
- スマホ・タブレット縦向きでもレイアウトが崩れない
