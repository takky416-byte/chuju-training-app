# v24 デプロイ手順

v23(デプロイ済み)からの変更です。Cloud Functionsの変更はないため、`hosting`のみでデプロイできます。

## 変更内容

### 表示のわかりやすさ改善

- 「11連ガチャ」ボタンを見出し＋説明の2行にして、「20コイン×10回分＝200コインで11回引ける！(+1回おまけ)」と金額の内訳がひと目でわかるようにしました
- デイリークエストの各クエストに、そのクエストの報酬「+10コイン」(達成後は「達成！+10コイン」)を明記しました
- 「最近の記録」を6件→15件表示に増やしました(縦に伸びすぎないよう、最大高さ620pxのスクロール枠付き)

## デプロイ

```bash
unzip -o aichi-jh-training-revised-v24.zip
npm install
npm run build
npx firebase-tools deploy --only hosting --project aichi-jh-training-507310
```

## 確認項目

- ガチャ画面で「11連ガチャ」ボタンに「200コインで11回引ける！(+1回おまけ)」の説明が表示される
- デイリークエストの各項目に「+10コイン」の表記がある
- 「最近の記録」に15件まで表示され、多い場合はスクロールできる
- 既存の機能に影響がないこと
