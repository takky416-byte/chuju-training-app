# v23 デプロイ手順

v22(デプロイ済み)からの変更です。Cloud Functionsの変更はないため、`hosting`のみでデプロイできます。

## 変更内容

### 10連ガチャ(11回)

- 通常の1回引くボタンの横に、200コイン(20コイン×10回分)で11回引ける「まとめて引く」ボタンを追加
- 10回に1回はレア以上確定という既存のルールは、11連の中でも同じロジックでそのまま機能します
- 結果はグリッド一覧で表示されます

### ガチャの排出について(調査結果・変更なし)

- 抽選ロジック自体に偏りはありませんでした
- ただし「人物」ガチャだけノーマル枠の景品数が4種(他は8種)と少なく、同じ人物が他のガチャの約2倍の頻度で出やすい設計になっています。気になる場合は景品追加で調整可能です(今回は変更していません)

### デイリークエスト

- 国語(漢字の書き・語句・漢字の読み、各5問)・社会(地理・歴史、各5問)・理科(生物・地学・物理・化学、各5問)・適性検査(30問)の4クエスト
- 各クエスト達成で10コイン、4つ全部達成でボーナス+20コインを自動付与
- 日付が変わると進捗は自然にリセット(その日の解答記録から都度計算)
- 一度もらったコインを二重に受け取らないよう、その日の達成状況を保存
- 学習画面のダッシュボードに進捗カードを追加(達成状況・科目ごとの内訳を表示)

## デプロイ

```bash
unzip -o aichi-jh-training-revised-v23.zip
npm install
npm run build
npx firebase-tools deploy --only hosting --project aichi-jh-training-507310
```

## 確認項目

- ガチャ画面に「まとめて引く」ボタンが表示され、200コインで11個の結果が一覧表示される
- 通常の1回ガチャは今まで通り動く
- ダッシュボードに「デイリークエスト」カードが表示される
- 基礎トレで国語系科目(漢字の書き・語句・漢字の読み)を5問ずつ解くと、国語クエストが達成表示になりコインが増える
- 適性検査を30問解くと、適性検査クエストが達成表示になる
- 4つ全部達成すると、ボーナス+20コインが加算される
- 翌日になるとクエストの進捗表示が0に戻る
- 既存の機能(履歴、日次メール、複数ファイル一括追加など)に影響がないこと
