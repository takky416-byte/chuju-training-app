# v22 デプロイ手順

v21（デプロイ済み）からの変更をまとめたものです。Cloud Functions（日次メール）に変更があるため、`functions`のデプロイも必要です。

## 変更内容

### 漢字の書きAI判定の正式化

- 「プロトタイプ」の文言を削除し、正式機能としての表示に変更
- AI判定の結果を「○正解／×不正解」のバッジ表示（アイコン付き・色分け）にして、ひと目でわかるように変更
- 漢字の答えのフォントサイズを拡大（38px→56px、スマホ・タブレット縦向きは33px→44px）

### 日次レポートの改善

- 間違えた問題の表記を改善。分野・科目を`【適性検査_数量・図形】`「基礎トレ_歴史】」のようにブラケット内に含め、あわせて正答も表示するようにしました（例：`【基礎トレ_歴史】ことわざの意味（正答：○○）`）。これまでの`kanji-basic-003-q009`のようなIDだけの表記や、`熟語の読み`のような内容が分からないカテゴリ名だけの表記を解消しています。
- 上記の表記を将来にわたって正しく保つため、解答した瞬間の問題タイトルと正答を履歴（`attempts`・`basicAttempts`）に保存するようにしました。**今回のデプロイ以降に解答した問題からこの情報が記録されます**（過去の履歴は従来通り問題バンクを参照して表示するため、問題が現存していれば引き続き正しく表示されます）。
- レポートに「分野・科目のバランス」セクションを追加。全体平均に比べて挑戦が著しく少ない分野・科目を一覧表示し、偏りに気づけるようにしました。

### バランスよく進める工夫（サイト本体）

- 「分野を選んで」「科目を選んで」の各ボタンに、挑戦が少ない分野・科目へ「・おすすめ」タグを表示するようにしました。

## デプロイ

```bash
unzip -o aichi-jh-training-revised-v22.zip
npm install
npm run build
npm install --prefix functions
npx firebase-tools deploy --only hosting,functions --project aichi-jh-training-507310
```

## 確認項目

- 漢字の書きで「答えを見る」を押すと、答え（大きな文字）の下にAI判定バッジ（○正解／×不正解）が表示される。「プロトタイプ」の文言が出ない
- 「分野を選んで」「科目を選んで」の各ボタンで、挑戦が少ない分野・科目に「・おすすめ」タグが表示される
- 適性検査・基礎トレをいくつか間違えてみて、翌日21:30の日次メールで「間違えた問題」に分野・科目と正答が表示される
- 日次メールに「分野・科目のバランス」セクションが表示される
- 既存の機能（複数ファイル一括追加、履歴、ガチャ、BGMなど）に影響がないこと
